// /admin/roles — Busca/edição de personagem real (delega no componente).
import { RolePersonagemTab } from "@/components/admin/RolePersonagemTab";

const RolesPage = () => (
  <div className="h-full overflow-y-auto p-6">
    <RolePersonagemTab />
  </div>
);

export default RolesPage;
